# Copy to .env for local overrides. In dev you normally leave VITE_API_URL empty
# (Vite proxies /api and /socket.io to the backend on :4000).
#
# In production (Vercel), set VITE_API_URL to your deployed backend origin:
# VITE_API_URL=https://cybernexus-api.onrender.com
VITE_API_URL=
